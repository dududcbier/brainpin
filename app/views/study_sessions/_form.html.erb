<%= form_with(model: study_session, local: true) do |form| %>
  <% if study_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study_session.errors.count, "error") %> prohibited this study_session from being saved:</h2>

      <ul>
      <% study_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
  <%= form.label :subtopic_id %>
  <%= form.collection_select :subtopic_id, Subtopic.all, :id, :name, include_blank: "Please select" %>

  <div class="field">
    <%= form.label :number_of_questions %>
    <%= form.number_field :num_questions %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
