<%= form_with(model: wrong_answer, local: true) do |form| %>
  <% if wrong_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wrong_answer.errors.count, "error") %> prohibited this wrong_answer from being saved:</h2>

      <ul>
      <% wrong_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field">
    <%= form.label :plausibility %>
    <%= form.number_field :plausibility %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
