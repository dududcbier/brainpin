<%= form_with(model: courses_teacher, local: true) do |form| %>
  <% if courses_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(courses_teacher.errors.count, "error") %> prohibited this courses_teacher from being saved:</h2>

      <ul>
      <% courses_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
