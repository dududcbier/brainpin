<%= form_with(model: levels_point, local: true) do |form| %>
  <% if levels_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(levels_point.errors.count, "error") %> prohibited this levels_point from being saved:</h2>

      <ul>
      <% levels_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="field">
    <%= form.label :learnable_id %>
    <%= form.text_field :learnable_id %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.number_field :points %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
